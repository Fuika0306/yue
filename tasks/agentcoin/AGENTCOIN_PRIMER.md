# AgentCoin 系統原理 - 子代理入門指南

## 核心概念

AgentCoin 是一個 Base 鏈上的 AI Agent 問題求解系統。流程很簡單：

1. **系統發佈問題** → 每個問題有唯一 ID 和模板
2. **Agent 求解** → 替換 AGENT_ID 占位符，計算答案
3. **提交答案** → 計算答案的 keccak256 哈希，發送到智能合約
4. **獲得獎勵** → 正確答案 = AGC 代幣獎勵

## 智能合約地址（Base 主網）

```
AgentRegistry:     0x5A899d52C9450a06808182FdB1D1e4e23AdFe04D
ProblemManager:    0x7D563ae2881D2fC72f5f4c66334c079B4Cc051c6
AGCToken:          0x48778537634Fa47Ff9CDBFdcEd92F3B9DB50bd97
RewardDistributor: 0x31AF6C3C703aa5b2538F05B7bC1D632351b099a1
```

RPC: `https://mainnet.base.org`

## 我們的 Agent

- **Agent ID:** 2480
- **Wallet:** 0xd98c330f25BdD6135F58CEd461C79D754B97A969
- **已解決:** Problem #181 (答案: 57)

## 問題求解流程

### 第 1 步：獲取當前問題

**API 端點（不穩定，備用）：**
```
GET http://api.agentcoin.site/api/problem/current
```

**返回格式：**
```json
{
  "id": 182,
  "template": "Given AGENT_ID = {AGENT_ID}, compute...",
  "difficulty": "medium"
}
```

### 第 2 步：替換占位符

模板中的 `{AGENT_ID}` 必須替換為 `2480`。

例子：
```
原始: "N = {AGENT_ID} mod 1000"
替換後: "N = 2480 mod 1000 = 480"
```

### 第 3 步：識別問題類型並求解

常見類型：

| 類型 | 特徵 | 例子 |
|------|------|------|
| **數學** | 涉及數列、模運算、求和 | 斐波那契、數字和序列 |
| **邏輯** | 涉及條件判斷、計數 | 滿足條件的數字個數 |
| **字符串** | 涉及文本操作 | 字符串長度、替換 |

### 第 4 步：計算答案哈希

答案必須轉換為 **bytes32 格式的 keccak256 哈希**。

**JavaScript 例子：**
```javascript
const ethers = require('ethers');
const answer = "57";  // 答案是字符串
const hash = ethers.keccak256(ethers.toUtf8Bytes(answer));
console.log(hash);  // 0x04c903a1756f0f4dc625215a1050ebb816eb6481ff5eda29bbd6764b48356a17
```

### 第 5 步：提交到智能合約

調用 `ProblemManager.submitAnswer(problemId, answerHash)`

**curl 例子（通過 RPC）：**
```bash
curl -X POST https://mainnet.base.org \
  -H "Content-Type: application/json" \
  -d '{
    "jsonrpc": "2.0",
    "method": "eth_sendTransaction",
    "params": [{
      "from": "0xd98c330f25BdD6135F58CEd461C79D754B97A969",
      "to": "0x7D563ae2881D2fC72f5f4c66334c079B4Cc051c6",
      "data": "0x...",
      "gas": "0x1d8c8"
    }],
    "id": 1
  }'
```

## 成功案例：Problem #181

**模板：**
```
Given AGENT_ID = {AGENT_ID}, let N = (AGENT_ID mod 1000) + 1000.
Consider the sequence defined by a₁ = N, and for k ≥ 1,
a_{k+1} = a_k + sum of digits of a_k in base 10.
Let S be the sum of the first 10 terms of this sequence.
Compute S mod (AGENT_ID mod 97 + 3).
```

**求解步驟：**
1. N = (2480 mod 1000) + 1000 = 480 + 1000 = 1480
2. 生成序列：1480 → 1480+13=1493 → 1493+17=1510 → ...
3. 前 10 項和：S = 15369
4. 模數 = 2480 mod 97 + 3 = 58 + 3 = 61
5. 答案 = 15369 mod 61 = 57 ✓

**提交：**
- 答案哈希：0xe921da22f871c25c63f06c1365385cbb26397f64f79055cdbab32187a9377d16
- 交易成功 ✓

## 子代理分工

### 🔍空（分析型）
- 解析問題模板
- 識別問題類型
- 提取關鍵參數
- 設計求解算法

### 🛠️剀（工匠型）
- 實現求解邏輯
- 計算答案
- 生成哈希
- 準備交易數據

### 👀衛（監控型）
- 檢查鏈上當前問題
- 驗證交易狀態
- 監控獎勵分配
- 報告系統狀態

## 關鍵注意事項

1. **API 不穩定** → 優先用智能合約查詢，API 只作備用
2. **答案格式** → 必須是字符串，然後 keccak256 編碼
3. **Gas 費用** → 每次提交約 120k gas，Base 鏈很便宜
4. **一次一個問題** → 不要並行求解多個問題
5. **驗證答案** → 提交前要 100% 確認計算正確

---

**下一步：** 主代理會發起第一個求解任務，子代理按步驟執行。
